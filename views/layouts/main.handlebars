<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>Tech Blog</title>
</head>

<body>
    <div class="text-white bg-dark">
        <nav class="navbar navbar-dark justify-content-start bg-dark">
        <a href="/" class="nav-item nav-link text-white active">Home</a>
        <a href="/dashboard" class="nav-item text-white nav-link active">Dashboard</a>
        {{#if logged_in}}
        <button id="logout" class="btn btn-dark" type="submit">Logout</button>
        {{else}}
        <a href="/login" class="nav-item nav-link text-white active">Login</a>
        {{/if}}
        </nav>
        <header class="pb-1 pl-1">
            <h1>Tech Blog</h1>
        </header>
    </div>
    <div>
        <section>
            {{{body}}}
        </section>
    </div>

</body>
{{#if logged_in}}
<script src="/js/logout.js"></script>
{{else}}
{{/if}}

</html>
